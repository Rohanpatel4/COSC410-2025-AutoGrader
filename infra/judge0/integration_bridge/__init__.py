"""
integration_bridge (CUSTOM — NOT FROM JUDGE0)

Lightweight bridge demo that:
- Combines a student submission + one test file,
- Sends to Judge0 (/submissions, wait=true),
- Writes a simple pass/fail grade JSON to io/outbound/.
"""